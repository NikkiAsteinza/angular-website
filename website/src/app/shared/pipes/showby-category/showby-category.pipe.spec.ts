import { ShowbyCategoryPipe } from './showby-category.pipe';

describe('ShowbyCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowbyCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
