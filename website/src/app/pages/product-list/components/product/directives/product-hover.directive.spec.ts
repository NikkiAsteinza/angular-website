import { ProductHoverDirective } from './product-hover.directive';

describe('ProductHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductHoverDirective();
    expect(directive).toBeTruthy();
  });
});
