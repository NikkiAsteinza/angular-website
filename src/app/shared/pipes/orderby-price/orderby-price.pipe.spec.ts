import { OrderbyPricePipe } from './orderby-price.pipe';

describe('OrderbyPricePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderbyPricePipe();
    expect(pipe).toBeTruthy();
  });
});
